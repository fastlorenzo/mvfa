<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="be.bernardi.mvforandroid.beta"
    android:versionCode="61"
    android:versionName="2.0.0" >

    <uses-sdk
        android:minSdkVersion="4"
        android:targetSdkVersion="4" />

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.READ_PHONE_STATE" />
    <uses-permission android:name="android.permission.SEND_SMS" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.READ_LOGS" />

    <application
        android:name=".ApplicationController"
        android:debuggable="true"
        android:icon="@drawable/icon"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".activities.SettingsActivity"
            android:label="@string/app_name" />
        <activity
            android:name=".activities.AuthActivity"
            android:theme="@android:style/Theme.Dialog" />

        <!-- Appwidget sample -->
        <receiver android:name=".widgets.ExampleAppWidgetProvider" >
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/appwidget_provider" />

            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
        </receiver>

        <activity android:name=".widgets.ExampleAppWidgetConfigure" >
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_CONFIGURE" />
            </intent-filter>
        </activity>

        <receiver
            android:name=".widgets.ExampleBroadcastReceiver"
            android:enabled="false" >
            <intent-filter>
                <action android:name="android.intent.ACTION_TIMEZONE_CHANGED" />
                <action android:name="android.intent.ACTION_TIME" />
            </intent-filter>
        </receiver>
        <!-- End appwidget -->

        <service android:name=".data.MVDataService" />

        <receiver android:name=".MainActivity" />
        <receiver android:name=".fragments.CreditFragment" />
        <receiver android:name=".fragments.UsageFragment" />
        <receiver android:name=".fragments.TopupsFragment" />
        <receiver android:name=".data.OnAlarmReceiver" />
        <receiver android:name=".data.OnBootReceiver" >
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
        </receiver>
        <receiver
            android:name=".widgets.WidgetProvider1x1"
            android:label="@string/widget_label_1x1" >
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
                <action android:name="be.bernardi.mvforandroid.beta.onWidgetClick" />
                <action android:name="be.bernardi.mvforandroid.beta.data.CreditUpdated" />
            </intent-filter>

            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/widget_info1x1" />
        </receiver>
        <receiver
            android:name=".widgets.WidgetProvider2x1"
            android:label="@string/widget_label_2x1" >
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
                <action android:name="be.bernardi.mvforandroid.beta.onWidgetClick" />
                <action android:name="be.bernardi.mvforandroid.beta.data.CreditUpdated" />
            </intent-filter>

            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/widget_info2x1" />
        </receiver>
    </application>

</manifest>